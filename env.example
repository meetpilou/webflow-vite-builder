# ============================================================
# Bunny CDN — Credentials
# ============================================================
# Copy this file to .env and fill in your values.
# Never commit .env to git.

# Storage Zone name (FTP username in Bunny dashboard)
BUNNY_STORAGE_NAME=my-storage-zone

# Storage Zone password (FTP & API Access → Password)
BUNNY_STORAGE_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Storage region (leave empty for default Falkenstein)
# Other options: uk, ny, la, sg, se, br, johannesburg
BUNNY_STORAGE_REGION=

# Account API key (Account Settings → API Key → Main API Key)
# Used to purge the CDN cache after upload
BUNNY_API_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Your Pull Zone CDN URL (no trailing slash)
BUNNY_CDN_URL=https://my-project.b-cdn.net
